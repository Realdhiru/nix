# source = ~/.config/hypr/myColors.conf
monitor=,preferred,auto,auto,bitdepth,10

# Set programs that you use
$terminal = wezterm
$fileManager = dolphin
$menu = rofi -show drun

### AUTOSTART ###
# exec-once = waybar

# Environment-variables

env = XCURSOR_SIZE,20
env = HYPRCURSOR_SIZE,20

### PERMISSIONS ###
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


### LOOK AND FEEL ###
general {
    gaps_in = 5
    gaps_out = 12

    border_size = 0

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

animations {
    enabled = true
    # Animation curves

    bezier = linear, 0, 0, 1, 1
    bezier = md3_standard, 0.2, 0, 0, 1
    bezier = md3_decel, 0.05, 0.7, 0.1, 1
    bezier = md3_accel, 0.3, 0, 0.8, 0.15
    bezier = overshot, 0.05, 0.9, 0.1, 1.1
    bezier = crazyshot, 0.1, 1.5, 0.76, 0.92
    bezier = hyprnostretch, 0.05, 0.9, 0.1, 1.0
    bezier = menu_decel, 0.1, 1, 0, 1
    bezier = menu_accel, 0.38, 0.04, 1, 0.07
    bezier = easeInOutCirc, 0.85, 0, 0.15, 1
    bezier = easeOutCirc, 0, 0.55, 0.45, 1
    bezier = easeOutExpo, 0.16, 1, 0.3, 1
    bezier = softAcDecel, 0.26, 0.26, 0.15, 1
    bezier = md2, 0.4, 0, 0.2, 1 # use with .2s duration
    # Animation configs
    animation = windows, 1, 3, md3_decel, popin 60%
    animation = windowsIn, 1, 3, md3_decel, popin 60%
    animation = windowsOut, 1, 3, md3_accel, popin 60%
    animation = border, 1, 5, default
    animation = fade, 1, 3, md3_decel
    # animation = layers, 1, 2, md3_decel, slide
    animation = layersIn, 1, 3, menu_decel, slide
    animation = layersOut, 1, 1.6, menu_accel
    animation = fadeLayersIn, 1, 2, menu_decel
    animation = fadeLayersOut, 1, 4.5, menu_accel
    # animation = workspaces, 1, 5, menu_decel, slide
    # animation = workspaces, 1, 2.5, softAcDecel, slide
    animation = workspaces, 1, 6, menu_decel, slidefade 15%
    animation = specialWorkspace, 1, 3, md3_decel, slidefadevert 15%
    # animation = specialWorkspace, 1, 2, menu_decel, fade
}

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    #pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1
    sensitivity = 0.6 # -1.0 - 1.0, 0 means no modification.
    accel_profile = adaptive
    touchpad {
        natural_scroll = true
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
# gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}



## Picture-in-Picture
#windowrule = float,title:^(.*[Pp]icture.*[Pp]icture.*)$
#windowrule = pin,title:^(.*[Pp]icture.*[Pp]icture.*)$
#windowrule = size 25% 25%,title:^(.*[Pp]icture.*[Pp]icture.*)$
#windowrule = move 73% 72%,title:^(.*[Pp]icture.*[Pp]icture.*)$

# Quickshell layers
#layerrule = blur true,match:namespace quickshell-launcher
#layerrule = ignore_alpha 0,match:namespace quickshell-launcher

#layerrule = blur true,match:namespace quickshell-notifications
#layerrule = ignore_alpha 0,match:namespace quickshell-notifications

#layerrule = blur true,match:namespace quickshell-control-center
#layerrule = ignore_alpha 0,match:namespace quickshell-control-center


### KEYBINDINGS ###

$mainMod = SUPER # Sets "Windows" key as main modifier

# bind = $mainMod, R, exec, hyde-shell screenrecord --start -- --audio
# bind = $mainMod ALT, R, exec, hyde-shell screenrecord --quit
#bindd = $mainMod Shift, Z, $d snip screen , exec, hyde-shell screenshot s # partial screenshot capture
#bindd = $mainMod+Ctrl+Shift, Z, $d freeze and snip screen, exec, hyde-shell screenshot sf # partial screenshot capture (frozen screen)
binddm = $mainMod, Z, $d hold to move window , movewindow
binddm = $mainMod, X, $d hold to resize window, resizewindow
bindd = Shift, F1, $d toggle fullscreen, fullscreen
#bindd = Shift, F2,  $d lock screen, exec, hyprlock
binddl  =  Shift, F3, $d pause media, exec, playerctl play-pause # toggle between media play and pause
binddl  =  Shift, F5, $d next media  , exec, playerctl next # media next
binddl  =  Shift, F4, $d  previous media , exec, playerctl previous # media previous
#bindd = $mainMod Ctrl, S, $d select shader, exec, pkill -x rofi || hyde-shell shaders.sh --select
bindd = $mainMod Shift, F, $d toggle pin on focused window, exec, hyde-shell window.pin
bindd = Control Alt, Delete, $d logout menu, exec, hyde-shell logoutlaunch
bindd = Control_L,Alt_R, $d toggle waybar and reload config, exec, hyde-shell waybar --hide

bind = SUPER SHIFT, C, exec, brave --ozone-platform=wayland --profile-directory="Default" --class=chatgpt --name=chatgpt --app=https://chat.openai.com

bind = SUPER, Y, exec, brave --ozone-platform=wayland --profile-directory="youtube" --class=youtube --app=https://youtube.com
# Monkeytype
# bind = SUPER, M, exec, brave --ozone-platform=wayland --profile-directory="Default" --class=monkeytype --app=https://monkeytype.com
# Notion
bind = SUPER, N, exec, brave --ozone-platform=wayland --profile-directory="Default" --class=notion --app=https://www.notion.so/02917993852a4825ab25a38c938de4f8

bind = SUPER, D, exec, brave --ozone-platform=wayland --profile-directory="Default" --class=whatsapp --app=https://web.whatsapp.com

bind = SUPER, G, exec, brave --ozone-platform=wayland --profile-directory="Default" --class=gemini --app=https://gemini.google.com/u/1/app?hl=en-IN&pageId=none

bind = SUPER CTRL, A, exec,brave --ozone-platform=wayland --profile-directory="Default" --class=claude --app=https://claude.ai/new

bind = SUPER SHIFT, D, exec, brave --ozone-platform=wayland --profile-directory="Default" --class=todo --name=todo --app=https://to-do.live.com/tasks/inbox

# Zoom In (Super + Scroll Up)
bind = SUPER, mouse_up, exec, hyprctl keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor | grep float | awk '{print $2 + 2.0}')

# Zoom Out (Super + Scroll Down)
bind = SUPER, mouse_down, exec, hyprctl keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor | grep float | awk '{print $2 - 2.0}')

# Reset Zoom (Super + Backspace)
bind = SUPER, Backspace, exec, hyprctl keyword cursor:zoom_factor 1.0

bindd = $mainMod Shift, F, $d toggle pin on focused window, exec, hyde-shell window.pin
bindd = Control Alt, Delete, $d logout menu, exec, hyde-shell logoutlaunch

$d=[$wm|Resize Active Window]
# Resize windows
bindde = $mainMod Shift, Right, $d resize window right , resizeactive, 30 0
bindde = $mainMod Shift, Left, $d resize window left, resizeactive, -30 0
bindde = $mainMod Shift, Up, $d resize window up, resizeactive, 0 -30
bindde = $mainMod Shift, Down, $d resize  window down, resizeactive, 0 30

bindd = Control Shift, Escape, $d system monitor , exec, hyde-shell system.monitor
$d=[$l|Rofi menus]
$rofi-launch=hyde-shell rofilaunch
bindd = $mainMod, A, $d application finder , exec, pkill -x rofi || $menu d
#bindd = $mainMod, TAB, $d window switcher , exec, pkill -x rofi || $menu w
#bindd = $mainMod Shift, E, $d file finder , exec, pkill -x rofi || $menu f
# bindd = $mainMod, slash, $d keybindings hint, exec, pkill -x rofi || hyde-shell keybinds_hint c # launch keybinds hint
#bindd = $mainMod, comma, $d emoji  picker , exec, pkill -x rofi || hyde-shell emoji-picker # launch emoji picker
#bindd = $mainMod, period, $d glyph picker , exec, pkill -x rofi || hyde-shell glyph-picker # launch glyph picker
#bindd = $mainMod, V, $d clipboard ,  exec, pkill -x rofi || hyde-shell cliphist -c # launch clipboard,
#bindd = $mainMod Shift, V, $d clipboard manager , exec, pkill -x rofi || hyde-shell cliphist # launch clipboard Manager
#bindd = $mainMod Shift, A, $d select rofi launcher , exec, pkill -x rofi || hyde-shell rofiselect # launch select menu
# bindd = $mainMod Alt, G, $d game mode , exec, hyde-shell gamemode # disable hypr effects for gamemode
# bindd = $mainMod Shift, G, $d open game launcher , exec, hyde-shell gamelauncher # run game launcher for steam and lutris
#bindd = $mainMod Shift, P, $d color picker, exec, hyprpicker -an # Pick color (Hex) >> clipboard#
#binddl = , Print, $d print all monitors , exec, hyde-shell screenshot p # all monitors screenshot capture

$rice=Theming and Wallpaper
$d=[$rice]
#bindd = $mainMod Alt, Right, $d next global wallpaper , exec, hyde-shell wallpaper -Gn # next global wallpaper
#bindd = $mainMod Alt, Left, $d previous global wallpaper , exec, hyde-shell wallpaper -Gp # previous global wallpaper
#bindd = $mainMod Shift, W, $d select a global wallpaper , exec, pkill -x rofi || hyde-shell wallpaper -SG # launch wallpaper select menu
#bindd = $mainMod Alt, Up, $d next waybar layout , exec, hyde-shell wbarconfgen n # next waybar mode
#bindd = $mainMod Alt, Down, $d previous waybar layout , exec, hyde-shell wbarconfgen p # previous waybar mode
#bindd = $mainMod Shift, R, $d wallbash mode selector , exec, pkill -x rofi || hyde-shell wallbashtoggle -m # launch wallbash mode select menu
#bindd = $mainMod Shift, T, $d select a theme, exec, pkill -x rofi || hyde-shell themeselect # launch theme select menu
#bindd = $mainMod+Shift, Y, $d select animations, exec, pkill -x rofi || hyde-shell animations --select # launch animations select menu
#bindd = $mainMod+Shift, U, $d select hyprlock layout, exec, pkill -x rofi || hyde-shell hyprlock --select # launch hyprlock layout select menu

$d=[$ws|Navigation|Relative workspace]
bindd = $mainMod Control, Right, $d change active workspace forwards  , workspace, r+1
bindd = $mainMod Control, Left, $d change active workspace backwards , workspace, r-1
$d=[$ws|Navigation]
bindd = $mainMod Control, Down, $d navigate to the nearest empty workspace , workspace, empty

bind = $mainMod, T, exec, $terminal
bind = $mainMod, C, exec, zeditor
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = SUPER, B, exec, brave
bind = $mainMod, W, togglefloating,
# bind = $mainMod, A, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bindd = $mainMod Alt, S, $d move to scratchpad (silent) , movetoworkspacesilent, special

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

### WINDOWS AND WORKSPACES ###

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
#windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
#windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
